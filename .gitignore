node_modules/
dist/
dist-server/
.env
.env.local
*.log
.DS_Store

# APK files (use GitHub Releases, not repo)
*.apk

# Firebase/Google services config (contains API keys)
**/google-services.json
**/firebase-adminsdk*.json

# Keystore and certificate files
*.keystore
*.jks
*.p12
*.pem
*.key

# Environment files (all variants)
.env.*
!.env.example

# Android build outputs
android/app/build/
app/android/build/
app/android/app/build/
app/android/capacitor-cordova-android-plugins/build/
app/android/.gradle/
app/android/local.properties

# Internal context (keep rules.md tracked for public repo)
.agentrune/
!.agentrune/rules.md

# Agent session state
.agentrune-session.json

# Claude Code project instructions (contains private paths)
CLAUDE.md

# Design plans (contain local paths)
docs/plans/

# Claude Code settings (may contain tokens)
.claude/

# IDE
.idea/
*.iml
.vscode/

# Coverage reports
coverage/
app/coverage/
desktop/coverage/

# Local QA / scratch outputs
.superpowers/
docs/superpowers/
tmp/
tmp-*.json
tmp-*.png
app/test-results/
cli/coverage/
.worktrees/
.agentrune-session.json
